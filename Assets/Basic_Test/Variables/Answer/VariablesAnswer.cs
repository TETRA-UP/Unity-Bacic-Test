using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VariablesAnswer : MonoBehaviour
{
    public int score = 0;
    public string text;

    // Start is called before the first frame update
    void Start()
    {
        score = 3;
    }

    // Update is called once per frame
    void Update()
    {

    }
}
